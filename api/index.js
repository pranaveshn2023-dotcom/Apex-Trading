// api/index.js — Unified Vercel Serverless Function (Hobby Plan Compliant: 1 Function Total)
// Routes all /api/* traffic directly to the Express application
import app from '../server/index.js';

export default app;
